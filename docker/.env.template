# ------------------------------------------------------------------------------
# 大香蕉 AI (Banana Pro) Docker 环境配置模版
# ------------------------------------------------------------------------------

# --- 后端配置 (Backend) ---
SERVER_PORT=8080
SERVER_HOST=0.0.0.0
TEMPLATES_REMOTE_URL=https://raw.githubusercontent.com/ShellMonster/Nano_Banana_Pro_Web/refs/heads/main/backend/internal/templates/assets/templates.json

# --- 生图模型配置 (Gemini) ---
PROVIDERS_GEMINI_API_KEY=
PROVIDERS_GEMINI_API_BASE=https://generativelanguage.googleapis.com
PROVIDERS_GEMINI_MODEL_ID=gemini-3-pro-image-preview
PROVIDERS_GEMINI_ENABLED=true

# --- 对话模型配置 (OpenAI/Chat) ---
# 注意：Viper 映射规则为 PROVIDERS_OPENAI_CHAT_xxx
PROVIDERS_OPENAI_API_KEY=
PROVIDERS_OPENAI_API_BASE=https://api.openai.com/v1
PROVIDERS_OPENAI_MODEL_ID=gemini-3-flash-preview
PROVIDERS_OPENAI_ENABLED=true

# --- 存储配置 (Storage) ---
STORAGE_LOCAL_DIR=storage
# 如果使用 OSS 需配置以下项
STORAGE_OSS_ENABLED=false
STORAGE_OSS_ENDPOINT=
STORAGE_OSS_ACCESS_KEY_ID=
STORAGE_OSS_ACCESS_KEY_SECRET=
STORAGE_OSS_BUCKET_NAME=
STORAGE_OSS_DOMAIN=

# --- 前端构建配置 (Frontend) ---
# 构建时注入，通常指向后端容器名或外部访问地址
#VITE_API_URL=http://localhost:8080/api/v1
